# No compilation necessary as of yet as Option is header-only

# file(GLOB_RECURSE SOURCES LIST_DIRECTORIES true *.hpp *.cpp)
#
# set(SOURCES ${SOURCES})
#
# set_target_properties(${MODULE_NAME}_lib PROPERTIES LINKER_LANGUAGE CXX)
#
# add_library(${MODULE_NAME}_lib STATIC ${SOURCES})
